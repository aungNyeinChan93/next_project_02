import React from "react";
import NavbarComponent from "./NavbarComponent";

const Navbar = async () => {
  return (
    <React.Fragment>
      <main>
        <NavbarComponent />
      </main>
    </React.Fragment>
  );
};

export default Navbar;
