
const add = (a: number, b: number): number => a + b;

const substract = (a: number, b: number): number => a - b;

// exports.multiply = (a: number, b: number) => a * b

module.exports = {
    substract, add
}